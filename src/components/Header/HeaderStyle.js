import styled from 'styled-components';

const HeaderStyle = styled.header`
  margin-bottom: 1.5rem;
  border-bottom: 1px solid ${(props) => props.theme.color.eerie_black};
`;

export default HeaderStyle;
